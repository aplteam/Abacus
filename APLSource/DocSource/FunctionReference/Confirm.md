# Confirm

Shows a modal confirmation box and blocks until the user picks a button.

~~~
R←D Confirm W
~~~

`D` is the Document. `W` is a namespace (or shortcut list) of properties — `Title`, `Message`,
`Buttons`. `R` is the caption of the button the user clicked (empty on Escape/cancel).
