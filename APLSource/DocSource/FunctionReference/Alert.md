# Alert

Shows a modal alert box with a message and an OK button.

~~~
R←D Alert Message
~~~

`D` is the Document. `Message` is the text to show (a character vector, or a vector of
character vectors for several paragraphs). `R` is `0`.
